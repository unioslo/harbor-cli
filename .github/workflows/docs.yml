name: build-docs

on:
  push:
    branches:
      - main
      - docs-dev
    paths:
      - "docs/**"
      - "mkdocs.yml"
      - ".github/workflows/docs.yml"
      # - "harbor_cli/**"

concurrency:
  group: docs-deploy

env:
  FORCE_COLOR: "1"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      - name: Ensure latest pip
        run: python -m pip install --upgrade pip

      - name: Install ourself
        run: |
          pip install -e .

      - name: Generate CLI help text for docs
        run: hatch run docs:python docs/scripts/get_help_text.py

      - name: Generate list of CLI commands for docs
        run: hatch run docs:python docs/scripts/get_command_list_full.py

      - name: Build documentation and publish
        run: hatch run docs:mkdocs gh-deploy --force
